# Legacy Windows heartbeat scripts

These two PowerShell scripts used to install a 12-minute Task Scheduler job on a
specific Windows machine path (`D:\Projects\otto-fresh`). They are **not** part
of the Vercel-hosted CRM and are kept only so history is not lost.

Do not reinstall them. Do not put real sign-in codes in any status file they
generate.
