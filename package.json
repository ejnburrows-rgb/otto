{
  "name": "otto-plumbing-crm",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000 --directory /home/daytona/codebase",
    "start": "python3 -m http.server 3000 --directory /home/daytona/codebase",
    "preview": "python3 -m http.server 3000 --directory /home/daytona/codebase",
    "build": "echo 'No build step — static single-file PWA' && ls -la index.html"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
