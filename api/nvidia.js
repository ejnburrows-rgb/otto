// Vercel serverless proxy for NVIDIA AI. Provider key remains server-only.
import { hasServerAuth, denyUnauthenticated } from './_lib/serverAuth.js';
const NVIDIA_URL=process.env.NVIDIA_URL||'https://integrate.api.nvidia.com/v1/chat/completions';const DEFAULT_MODEL='meta/llama-3.3-70b-instruct';
export default async function handler(req,res){if(!(await hasServerAuth(req))){denyUnauthenticated(res);return;}return nvidiaHandler(req,res);}
export async function nvidiaHandler(req,res){if(req.method!=='POST'){res.status(405).json({error:'method_not_allowed'});return;}const key=process.env.NVIDIA_API_KEY;if(!key){res.status(503).json({error:'no_server_key'});return;}try{let body=req.body;if(body==null||typeof body==='string'){const raw=typeof body==='string'?body:await readRaw(req);body=raw?JSON.parse(raw):{};}if(!body.model)body.model=process.env.NVIDIA_MODEL||DEFAULT_MODEL;if(body.stream)body.stream=false;const upstream=await fetch(NVIDIA_URL,{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+key,'Accept':'application/json'},body:JSON.stringify(body)});const text=await upstream.text();res.status(upstream.status);res.setHeader('Content-Type','application/json');res.send(text);}catch{res.status(502).json({error:'upstream_error'});}}
function readRaw(req){return new Promise((resolve,reject)=>{let data='';req.on('data',(c)=>{data+=c;});req.on('end',()=>resolve(data));req.on('error',reject);});}
