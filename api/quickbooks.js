// QuickBooks Online OAuth + sync stub.
import { hasServerAuth, denyUnauthenticated } from './_lib/serverAuth.js';
const QB_AUTH='https://appcenter.intuit.com/connect/oauth2';
export default async function handler(req,res){const clientId=process.env.QB_CLIENT_ID;const clientSecret=process.env.QB_CLIENT_SECRET;const configured=!!(clientId&&clientSecret);
if(req.method==='GET'){const action=req.query?.action||'status';if(action==='auth_url'){if(!configured)return res.status(503).json({error:'not_configured'});const redirect=process.env.QB_REDIRECT_URI||`${req.headers['x-forwarded-proto']||'https'}://${req.headers.host}/api/quickbooks?action=callback`;const url=`${QB_AUTH}?client_id=${encodeURIComponent(clientId)}&redirect_uri=${encodeURIComponent(redirect)}&response_type=code&scope=${encodeURIComponent('com.intuit.quickbooks.accounting')}&state=otto`;return res.status(200).json({ok:true,authUrl:url});}return res.status(200).json({ok:true,configured,connected:!!process.env.QB_REFRESH_TOKEN});}
if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});let body=req.body;if(body==null||typeof body==='string'){try{body=JSON.parse(body||'{}');}catch{body={};}}const action=body.action||'status';if(action==='status')return res.status(200).json({ok:true,configured,connected:!!process.env.QB_REFRESH_TOKEN});if(action==='sync'){if(!(await hasServerAuth(req))){denyUnauthenticated(res);return;}const out=runSync(configured,body);return res.status(out.status).json(out.body);}return res.status(400).json({error:'unknown_action'});}
export function runSync(configured,body){if(!configured||!process.env.QB_REFRESH_TOKEN)return{status:503,body:{error:'not_connected'}};const count=Array.isArray(body.records)?body.records.length:0;return{status:200,body:{ok:true,synced:count,kind:body.kind||'invoices',note:'Sync stub — wire Intuit API when credentials are live.'}};}
