# What must NOT be published.
#
# vercel.json sets outputDirectory to ".", so before this file existed the whole
# repository was served from the public domain. Verified on production:
# docs/STATUS.md, docs/OWNER-MANUAL-STEPS.md, AGENTS.md, scripts/*.mjs and
# supabase/migrations/0001_init_schema.sql all returned 200.

# Working documents and process
docs/
*.md
!api/**/*.md
.github/
.githooks/
.vscode/
.cursorrules
.mcp.json

# Vercel needs the Node test/tool scripts during the build so it can run the full
# suite. The build wrapper removes the entire scripts/ directory after verification
# and before the deployment output is published.
scripts/*
!scripts/vercel-build.mjs
!scripts/*.mjs
evidence/
outputs/

# Database schema and migrations
supabase/

# Superseded material
legacy/

# Local environment
.env
.env.*
node_modules/
